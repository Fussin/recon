apiVersion: v1
kind: Secret
metadata:
  name: autonomouspen-ai-secrets
  namespace: autonomouspen-ai
type: Opaque
data:
  # Add your secret data here
  # For example:
  # DB_PASSWORD: "your-password"
  # API_KEY: "your-api-key"
